import Vue from 'vue';
import Vuetify from 'vuetify/lib/framework';
import { VBtn } from 'vuetify/lib'

Vue.use(Vuetify, {
    components: {
        VBtn
    }
});

export default new Vuetify({
    
});
